{
  "name": "frontal-mcp-server",
  "version": "1.0.0",
  "description": "Initial MCP server setup",
  "license": "MIT",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "lint": "biome lint src",
    "format": "biome format src --write",
    "type-check": "tsc --noEmit",
    "prepare": "husky"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@changesets/cli": "latest",
    "@frontal/style-guide": "github:frontal-labs/style-guide",
    "@types/node": "latest",
    "husky": "latest",
    "typescript": "latest"
  }
}